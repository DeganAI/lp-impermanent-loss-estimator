{
  "name": "lp-impermanent-loss-estimator",
  "version": "1.0.0",
  "description": "Calculate IL and fee APR for LP positions via x402",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@lucid-dreams/agent-kit": "^0.2.24",
    "hono": "^4.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0"
  }
}
